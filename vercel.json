{
  "buildCommand": "chmod +x node_modules/.bin/* 2>/dev/null; npx expo export -p web",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
